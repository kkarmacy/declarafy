{
  "name": "declarafy-web",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": "20" },
  "scripts": {
    "test": "node --test tests/*.test.js && node scripts/verify-deployment-structure.js"
  }
}
