// Audited income-tax regime, annual return, loss carryforward and suspension layer.
(function(global){'use strict';const money=v=>'S/ '+Number(v||0).toLocaleString('es-PE',{minimumFractionDigits:2,maximumFractionDigits:2});const note=t=>'<div style="margin-top:12px;padding:10px 12px;border:1px solid var(--border);border-radius:8px;color:var(--muted);font-size:12px;line-height:1.5">📌 '+t+'</div>';
function calcRentaAnual(){const year=Number(document.getElementById('renta_year')?.value||0),cat=document.getElementById('renta_cat')?.value||'',income=Number(document.getElementById('renta_ingresos')?.value||0),cost=Number(document.getElementById('renta_costos')?.value||0),expense=Number(document.getElementById('renta_gastos')?.value||0),box=document.getElementById('rentaResult');if(!box)return;if(income<=0){box.style.display='none';return}const accounting=income-cost-expense;box.style.display='block';box.innerHTML='<div class="sunat-api-result"><table><tr><td>Ejercicio / categoría</td><td>'+year+' / '+cat+'</td></tr><tr><td>Ingresos</td><td>'+money(income)+'</td></tr><tr><td>Costos + gastos informados</td><td>'+money(cost+expense)+'</td></tr><tr><td>Resultado preliminar</td><td><strong>'+money(accounting)+'</strong></td></tr></table>'+note('No se aplica una única fórmula a tercera, cuarta y quinta categoría. La versión anterior restaba 7 UIT incluso a tercera categoría y aportes previsionales como deducción general. Declarafy exige ahora completar la conciliación tributaria y usar el cálculo específico de cada categoría.')+'</div>';}
function calcRegimen(){const income=Number(document.getElementById('regIngresos')?.value||0),activity=document.getElementById('regActividad')?.value||'',box=document.getElementById('regimenResult');if(!box)return;if(income<=0){box.style.display='none';return}box.style.display='block';box.innerHTML='<div class="sunat-api-result"><table><tr><td>Ingresos informados</td><td>'+money(income)+'</td></tr><tr><td>Actividad</td><td>'+activity+'</td></tr></table>'+note('El selector ya no ordena regímenes por “menor impuesto” ni recomienda automáticamente uno. NRUS, RER, RMT y Régimen General tienen límites, sujetos y actividades excluidas, comprobantes permitidos y reglas distintas. Debe verificarse elegibilidad antes de comparar carga tributaria.')+'</div>';}
function calcRus(){const income=Number(document.getElementById('rus_ingresos')?.value||0),invoice=document.getElementById('rus_factura')?.value||'',activity=document.getElementById('rus_actividad')?.value||'',box=document.getElementById('rusResult');if(!box)return;if(income<=0){box.style.display='none';return}let category=income<=5000?'Categoría 1 — cuota S/ 20':income<=8000?'Categoría 2 — cuota S/ 50':'Excede el límite mensual ordinario';box.style.display='block';box.innerHTML='<div class="sunat-api-result"><table><tr><td>Ingreso mensual informado</td><td>'+money(income)+'</td></tr><tr><td>Referencia por monto</td><td><strong>'+category+'</strong></td></tr><tr><td>Actividad</td><td>'+activity+'</td></tr><tr><td>¿Indica que emite factura?</td><td>'+invoice+'</td></tr></table>'+note('La categoría por monto no confirma acogimiento al NRUS. También deben verificarse compras, activos/locales, tipo de contribuyente, actividades excluidas y comprobantes permitidos.')+'</div>';}
function calcPerdida(){const year=Number(document.getElementById('perdida_anio')?.value||0),loss=Number(document.getElementById('perdida_monto')?.value||0),box=document.getElementById('perdidaResult');if(!box)return;if(loss<=0){box.style.display='none';return}box.style.display='block';box.innerHTML='<div class="sunat-api-result"><table><tr><td>Pérdida tributaria informada</td><td>'+money(loss)+'</td></tr><tr><td>Ejercicio de origen</td><td>'+year+'</td></tr></table>'+note('Art. 50 LIR: primero debe elegirse y documentarse el sistema de compensación aplicable. No se usa un porcentaje libre ingresado por el usuario. Sistema A y Sistema B tienen reglas y horizontes distintos; la compensación debe calcularse sobre renta neta tributaria futura, no sobre “ingresos” genéricos.')+'</div>';}
function calcSuspension(){const type=document.getElementById('susp_tipo')?.value||'',rem=Number(document.getElementById('susp_rem')?.value||0),start=document.getElementById('susp_inicio')?.value||'',end=document.getElementById('susp_fin')?.value||'',box=document.getElementById('suspResult');if(!box)return;if(!(rem>0&&start&&end)){box.style.display='none';return}const a=new Date(start+'T00:00:00'),b=new Date(end+'T00:00:00');if(!(b>=a)){box.style.display='none';return}const days=Math.floor((b-a)/86400000)+1;box.style.display='block';box.innerHTML='<div class="sunat-api-result"><table><tr><td>Tipo declarado</td><td>'+type+'</td></tr><tr><td>Período</td><td>'+start+' → '+end+' ('+days+' días calendario)</td></tr><tr><td>Remuneración informada</td><td>'+money(rem)+'</td></tr></table>'+note('No se concluye automáticamente “con goce”, descuento, CTS o vacaciones a partir del nombre de la suspensión. Enfermedad, accidente, licencias, huelga y suspensión perfecta tienen reglas, subsidios y efectos laborales diferentes que deben validarse con el supuesto concreto.')+'</div>';}
function addRmtRer(){const host=document.getElementById('ptRegimen')||document.getElementById('ptRegimenes');if(!host||document.getElementById('rmtRerAudit'))return;const s=document.createElement('section');s.id='rmtRerAudit';s.innerHTML='<div class="sec-title">🎯 RMT vs RER — Comparación de requisitos</div><p style="color:var(--muted)">La comparación se realiza por elegibilidad y mecánica tributaria, no por una recomendación automática basada solo en el impuesto estimado.</p>'+note('RER aplica pago mensual definitivo sobre ingresos netos y tiene límites/exclusiones. RMT determina pagos a cuenta y regularización anual sobre renta neta, con límites propios. Verifica actividad, ingresos, compras y demás requisitos del ejercicio.');host.appendChild(s);}
function install(){global.calcRentaAnual=calcRentaAnual;global.calcRegimen=calcRegimen;global.calcRus=calcRus;global.calcPerdida=calcPerdida;global.calcSuspension=calcSuspension;addRmtRer();}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();})(window);